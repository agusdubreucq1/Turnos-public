export enum Dias {
  lunes = 'lunes',
  martes = 'martes',
  miercoles = 'miercoles',
  jueves = 'jueves',
  viernes = 'viernes',
  sabado = 'sabado',
  domingo = 'domingo',
}

export enum Status {
  pending = 'pending',
  confirmed = 'confirmed',
  cancelled = 'cancelled',
}

export enum Duration{
  '30min' = 30,
  '60min' = 60,
  '90min' = 90,
  '120min' = 120,
  '150min' = 150,
  '180min' = 180
}